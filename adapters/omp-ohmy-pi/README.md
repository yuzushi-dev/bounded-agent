# Optional oh-my-pi adapter

This directory is a host-specific optional hook adapter. The bounded runtime and CLI do not import or require it.

Install it only when the host provides the matching hook API, using `bounded install --adapter omp-ohmy-pi`.
